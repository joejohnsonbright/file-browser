import { ProcessedItem, SortOption } from '../types/files';

export function sortFiles(
  fileData: ProcessedItem[],
  rawData: any[],
  sortOption: SortOption,
  ascending: boolean = false
): ProcessedItem[] {
  // Placeholder implementation - returns empty array
  return [];
}
